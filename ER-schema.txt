Employees(_idnr_, name)

Departments(_name_, abbreviation)
    UNIQUE abbreviation

Programs(_name_, abbreviation)
    UNIQUE abbreviation

Hosts(_department_, _program_)
    department -> Department.name
    program -> Program.name

Students(_idnr_, name, login, program)
    program -> Program.name

Branches(_name_, _program_)
    program -> Program.name

WorksFor(_employee_,_department_)	
    employee -> Employees.idnr
    department -> Departments.name

Branches(_name_,_program_)
    program -> program.name

Courses(_code_, name, credits)
    department -> Departments.name

MandatoryProgram(_course_, _program_)
    courses -> Courses.code
    program -> Programs.name

MandatoryBranch(_course_, _branch_, _program_)
    course -> Course.code
    (branch, program) -> Branches.(name, program)

GivenBy(_department_,_course_)
    department -> Departments.name
    course -> Courses.code

LimitedCourse(_code_, seats)
    code -> Course.code

BelongToProgram(_student_, program)
    student -> Students.idnr
    program -> Programs.code

Registered(taken, administrator, prerequisites, _student_, _course_) 
    student -> Students.idnr
    course -> Courses.code

Classifications(_name_)

Classified(_course_, _classification_)
    course -> Course.code
    classification -> Classification.name

WaitingList(_student_, _course_, position)
    student -> Student.idnr
    course -> LimitedCourse.code
    UNIQUE (position, course)






// need?
PassedCourses(credited,_student_,_course_)
student -> student.idnr
course ->course.code




graduateStudent(_student_,_course_,passedCourses,_branch_,program)




student ->student.idnr
course ->course.code
branch ->branch.name
program -> program.code
passedCourses ->passedCourses.credited